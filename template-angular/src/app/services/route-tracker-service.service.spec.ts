import { RouteTrackerService } from './route-tracker-service.service';

describe('RouteTrackerService', () => {
  it('should create an instance', () => {
    expect(new RouteTrackerService()).toBeTruthy();
  });
});
